export type ProductTypes = {
    title: string;
    price: number;
    thumbnail: string;
    id: number;
}

export type ManageTimeOutTypes = {
    search: undefined | number,
    scroll: undefined | number,
} 